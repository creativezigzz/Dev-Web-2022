import React, { useState } from "react";
import { ScrollView, TextInput, View, Text, FlatList, Image, StyleSheet } from 'react-native';

const Test = () => {
    return (
        <View style={{flexDirection:'row', justifyContent:'space-between'}}>
            <Text style={{flex:6}}>cl</Text>
            <Text style={{flex:11}}>ca</Text>
            <Text style={{flex:2}}>z</Text>
            <Text style={{flex:4}}>z</Text>
        </View>
    )
}
export default Test;